# TFSB v0.2.0 Release Readiness Record

This internal record summarizes the qualification facts and cryptographic identities for the Theme Forge Stellar Burst v0.2.0 Release Candidate requalified in TFSB26A-R1 following clean-checkout test bootstrap correction.

## Release Identifiers & Commit Hashes

- **Development Release Source SHA (`DEV_RELEASE_SOURCE_SHA`):** `35ee041498c60b8b6c0621b64b72abbcafa8de47`
- **Development Release Source Tree (`DEV_RELEASE_SOURCE_TREE`):** `ec0b31ed5eebe47aa94046a376e0428573439d09`
- **Development Public Fix Source SHA (`DEV_PUBLIC_FIX_SOURCE_SHA`):** `e2b972f903256ccd6ad93a56c0be65554a0850a0`
- **Development Public Fix Source Tree (`DEV_PUBLIC_FIX_SOURCE_TREE`):** `1b69bda53a940be0d2c5ec4805f39f00b5154582`
- **Public Root Commit SHA (`PUBLIC_ROOT_COMMIT`):** `40fac6ed536fa656e32365f6ff47bfcd5e58c6a5` (`Release v0.1.0`)
- **Public v0.2.0 Source Commit SHA (`PUBLIC_V020_RELEASE_COMMIT`):** `200867d361cf9fcb78daed1c4a092913d144af33` (`Release v0.2.0`)
- **Public Head SHA (`PUBLIC_V020_HEAD_SHA` / `NEW_PUBLIC_FIX_SHA`):** `ec68c0dff9a9412a4b0b1d899120324701a893b9` (`Fix TFSB clean-checkout test bootstrap`)
- **Public Head Tree SHA (`PUBLIC_V020_HEAD_TREE`):** `cdd6bd4068ce0c527a797e11190b27f7d4a415db`
- **Package Name:** `@knowledge-forge-ai/theme-forge-stellar-burst`
- **Target Package Version:** `0.2.0`
- **Node Requirement:** `>=22`
- **Steward:** `Samuel Leighton Lair <lair001@gmail.com>`
- **License:** `AGPL-3.0-or-later` (Dual commercial licensing available)

> [!NOTE]
> Public history contains exactly three commits:
> 1. `40fac6ed536fa656e32365f6ff47bfcd5e58c6a5` — `Release v0.1.0`
> 2. `200867d361cf9fcb78daed1c4a092913d144af33` — `Release v0.2.0`
> 3. `ec68c0dff9a9412a4b0b1d899120324701a893b9` — `Fix TFSB clean-checkout test bootstrap`
> 
> Public history was not rewritten. The transparent corrective commit ensures `npm test` executes self-contained via `pretest: npm run build`, and CI includes `npm audit --omit=dev`.

## Public CI Runs

- **Run ID 32605138848 (`200867d361cf9fcb78daed1c4a092913d144af33`):** `visual` passed; `verify` failed at `npm test` because compiled `dist/index.js` was missing before build (superseded).
- **Run ID 32606047280 (`ec68c0dff9a9412a4b0b1d899120324701a893b9`):**
  - `verify` (ID `97111127968`): Passed in 22s (`typecheck`, `pretest`, `build`, `vitest`, `audit --omit=dev`).
  - `visual` (ID `97111128045`): Passed in 1m52s (all 114 visual tests across Chromium, Firefox, WebKit).

## Runtime Dependencies & Closures

- `@xmldom/xmldom`: `0.9.12` (MIT)
- `fflate`: `0.8.3` (MIT)
- `smol-toml`: `1.8.0` (BSD-3-Clause)
- Zero optional or peer dependencies.

## Verification & Qualification Summary

- **Typecheck:** Clean (`tsc -p tsconfig.json && tsc -p tsconfig.visual.json && tsc -p tsconfig.tools.json`) from a clean checkout without pre-existing `dist/`.
- **Self-Contained Test Execution:** `npm test` automatically triggers `pretest` (`npm run build`), resolving `dist/index.js` for child-process Vitest runners.
- **Dogfood Audit Tool:** Loads compiled product from `dist/index.js` via `npm run build && node tools/audit-dogfood-corpus.mjs`, verified against checked-in fixtures (exit 0).
- **Unit & Integration Suite:** 25 Vitest files, 310 tests passed (0 failures).
- **Visual Regression Suite:** 114 Playwright visual tests passed across Chromium, Firefox, WebKit (0 pixel deviations).
- **Production Audit:** 0 production vulnerabilities (`npm audit --omit=dev`).
- **CLI Self-Reporting:** `tfsb --version` returns `0.2.0`; `tfsb --help` complete.
- **npm Registry Preflight:** Version `0.2.0` confirmed unused.

## Package Tarball Properties

- **Path:** `~/Documents/agent/outbox/theme-forge-stellar-burst/TFSB26A-R1/package/knowledge-forge-ai-theme-forge-stellar-burst-0.2.0.tgz`
- **File Count:** 67 files
- **Packed Size:** 98,729 bytes
- **Unpacked Size:** 451,804 bytes
- **Shasum (SHA-1):** `3cbf866358382e38d00aa08f5c6dcf3292693590`
- **Integrity (SHA-512):** `sha512-zarAqiapSAPRI45wePp7KhWfFQJx/3MRCUIgwvdArifxg+80PQKWKDgxtcc5wkcnm1Vfv5IlMPnzZf4EQRVsGw==`

## Consumer Lifecycle Qualification

Installed candidate tarball into an isolated consumer repository and executed the full bounded lifecycle using only the installed package:
1. `tfsb --version` (0.2.0) & `tfsb --help` (exit 0)
2. `tfsb import <zip> --root . --companion README.md --companion brand-README.md --record-provenance`
3. `tfsb reconcile <zip> --root . --companion README.md --companion brand-README.md --json` (clean exit 0)
4. `tfsb fmt` & `tfsb fmt --check` (exit 0)
5. `tfsb build --root .` (emitted `tfsb-build-v3` receipt in `brand/dist/.tfsb-build.json`)
6. `tfsb install --root .` (atomic multi-destination installation)
7. `tfsb check --json` & `tfsb list --json` (exit 0)
8. `tfsb diff` with `--provenance`, `--archive`, `--build`, and `--install` (exit 0)
9. `tfsb bundle --output bundle.zip --json` (deterministic store-only ZIP) & verified `--manifest` import
10. `tfsb preview --json` (static, scriptless, offline gallery)
11. Tampered installed file detected by `tfsb check --json` (exit 2, `status: drift`)
12. Reinstall restored clean state (exit 0)
13. Final clean `tfsb check --json` (exit 0)

## Public Composition & Exclusions

- **Included in Public Candidate:** `.github/**`, `src/**`, `test/**` (including fixtures and visual baselines), `tools/**`, `corpus/**`, `docs/architecture/**`, `docs/decisions/**`, `docs/evaluation/**`, `docs/examples/**`, `docs/implementation/**`, `docs/releases/v0.1.0.md`, `docs/releases/v0.2.0.md`, package config and legal files.
- **Excluded from Public Candidate:** `.agents/**`, `.claude/**`, `docs/releases/*-publication.md`, `docs/releases/*-public-composition.md`, `docs/releases/*-readiness.md`, `dist/**`, `node_modules/**`, `*.tgz`, `.tfsb-preview/**`.

## Known v0.2 Limitations

- Bounded declarative SVG schema-1 profile (unsupported elements fail closed).
- Local filesystem ZIP archives only (no URL fetching).
- Project scale bounded up to 128 mutating assets (full icon warehouses not a target).
- No raw XML escape hatch.
- Mutation commands (`import`, `build`, `install`) intentionally reject `--json`.
- Preview gallery is scriptless and offline.

## Remaining Actions for Publication (TFSB26B)

- **TFSB26B:**
  1. Clone public repo from `PUBLIC_V020_HEAD_SHA` (`ec68c0dff9a9412a4b0b1d899120324701a893b9`).
  2. Rebuild/requalify package matching exact tarball hashes.
  3. Publish `@knowledge-forge-ai/theme-forge-stellar-burst@0.2.0` to npm.
  4. Perform clean registry install smoke test.
  5. Create and push annotated tag `v0.2.0` to public repository at `PUBLIC_V020_HEAD_SHA`.
  6. Create GitHub Release `v0.2.0`.

